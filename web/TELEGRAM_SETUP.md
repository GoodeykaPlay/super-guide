# Настройка игры в Telegram

## Шаг 1: Создание Telegram бота

1. Открой Telegram и найди **@BotFather**
2. Отправь команду `/newbot`
3. Следуй инструкциям:
   - Придумай имя бота (например: "Goodeyka Games")
   - Придумай username (например: "goodeyka_games_bot")
4. Сохрани **токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройка Web App

1. Отправь BotFather команду `/newapp`
2. Выбери своего бота из списка
3. Укажи:
   - **Title**: Goodeyka Bolt Catcher
   - **Short name**: bolt-catcher
   - **Description**: Поймай болты, избегай бомбы! Классическая аркада в стиле 90-х
   - **Photo**: Загрузи скриншот игры (640x360px)
   - **Web App URL**: `https://твой-username.github.io/Goodeyka/web/game.html` (замени на свой URL после деплоя)
   - **GIF**: Опционально, можно добавить анимацию игры

## Шаг 3: Деплой игры на GitHub Pages

### Вариант A: Через GitHub (рекомендуется)

1. Создай репозиторий на GitHub (если ещё нет)
2. Загрузи туда проект
3. Перейди в **Settings** → **Pages**
4. В **Source** выбери ветку (например, `main`) и папку `/web`
5. Сохрани
6. Твой URL будет: `https://твой-username.github.io/Goodeyka/web/game.html`

### Вариант B: Через другой хостинг

- **Vercel**: Перетащи папку `web` в Vercel
- **Netlify**: Перетащи папку `web` в Netlify
- **GitHub Pages**: Следуй инструкциям выше

## Шаг 4: Обновление Web App URL в боте

После деплоя обнови URL в BotFather:
1. Отправь `/myapps`
2. Выбери своё приложение
3. Выбери **Edit** → **Web App URL**
4. Вставь новый URL

## Шаг 5: Создание бота для уведомлений об обновлениях (опционально)

Создай простой Python/Node.js скрипт для отправки уведомлений:

### Python пример (bot_notifier.py):

```python
import requests
import time

BOT_TOKEN = "твой-токен-бота"
CHAT_IDS = []  # Список ID чатов/пользователей для уведомлений

def send_update(message):
    url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
    for chat_id in CHAT_IDS:
        requests.post(url, json={
            "chat_id": chat_id,
            "text": message,
            "parse_mode": "HTML"
        })

# Пример использования:
# send_update("🎮 <b>Обновление игры!</b>\n\nДобавлены новые функции...")
```

### Node.js пример (bot_notifier.js):

```javascript
const https = require('https');

const BOT_TOKEN = 'твой-токен-бота';
const CHAT_IDS = []; // Список ID для уведомлений

function sendUpdate(message) {
  const url = `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`;
  CHAT_IDS.forEach(chatId => {
    const data = JSON.stringify({
      chat_id: chatId,
      text: message,
      parse_mode: 'HTML'
    });
    
    const options = {
      hostname: 'api.telegram.org',
      path: `/bot${BOT_TOKEN}/sendMessage`,
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Content-Length': data.length
      }
    };
    
    const req = https.request(options);
    req.write(data);
    req.end();
  });
}

// sendUpdate('🎮 <b>Обновление!</b>\n\nНовые функции...');
```

## Шаг 6: Тестирование

1. Открой своего бота в Telegram
2. Нажми на кнопку с игрой (или отправь команду `/start`)
3. Проверь, что игра загружается и работает

## Полезные команды для бота

- `/setmenubutton` - Настроить кнопку меню с игрой
- `/setdescription` - Описание бота
- `/setabouttext` - О боте
- `/setuserpic` - Аватар бота

## Безопасность

⚠️ **Важно**: Никогда не публикуй токен бота в публичных репозиториях!
Используй переменные окружения или секреты GitHub.

## Дополнительные функции

Можно добавить:
- Таблицу лидеров (через бота)
- Ежедневные награды
- Достижения
- Обмен рекордами между друзьями



